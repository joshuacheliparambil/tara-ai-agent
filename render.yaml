services:
  - type: web
    name: tara-ai-agent
    runtime: docker
    plan: free
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: PORT
        value: 3000
      - key: TRACE_FILE
        value: /tmp/tara-trace.ndjson

